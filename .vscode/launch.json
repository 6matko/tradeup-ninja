{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Dev SSR",
            "request": "launch",
            "command": "npm run dev:ssr",
            "cwd": "${workspaceFolder}"
        }
    ]
}